import 'package:flutter/material.dart';

class AlbumFailureView extends StatelessWidget {
  const AlbumFailureView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tente novamente"),
    );
  }
}
